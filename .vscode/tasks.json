{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build and run",
            "command": "gcc -o ${fileBasenameNoExtension} ${file} && ./${fileBasenameNoExtension}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
